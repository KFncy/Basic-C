1.请实现函数: int isPowerOfTwo(unsigned int n)。
该函数接收一个非负数n，请判断n是否为2的幂次方。
如果是返回真；否则返回假。为简单起见，不考虑n为负数的情况。如果存在一个整数X使得x = 2^n，则认为n是2的幂次方。

2.在一个整型数组中，只有一个数字出现一次，其他数组都是成对出现的，请找出那个只出现一次的数字。

例如：

数组中有：1 2 3 4 5 1 2 3 4，只有5出现一次，其他数字都出现2次，找出5

3.写一个函数返回参数二进制中 1 的个数。

比如： 15    0000 1111    4 个 1

4.交换两个变量（不创建临时变量）

